const mongoose = require('mongoose');

require('../mongodb_helper');
const Station = require('../../models/station');

describe('Station', () => {
  it('returns list', () => {
    console.log('');
  });
});
